# Iteration 12 - 2026-05-05

## 上一轮错误修复
- [x] 无新增错误

## 本轮优化方向

### 1. 修复 vitals.ts 调试代码
- **文件**: `src/lib/vitals.ts`
- **问题**: `console.log` 输出 Web Vitals 指标，生产环境不应输出
- **修复**: 移除 console.log，或改为仅在 development 输出（已有限制但可优化）

### 2. 修复 product-card.tsx 硬跳转
- **文件**: `src/components/product-card.tsx`
- **问题**: 使用 `window.location.href` 进行页面跳转，破坏 SPA 体验
- **修复**: 使用 Next.js `useRouter` 进行客户端导航

### 3. 修复 products-content.tsx 直接使用 window
- **文件**: `src/app/products/products-content.tsx`
- **问题**: 使用 `window.location.pathname` 获取路径
- **修复**: 使用 Next.js `usePathname` hook

### 4. 修复 search-dialog.tsx AnimatePresence 使用
- **文件**: `src/components/search-dialog.tsx`
- **问题**: `if (!open) return null` 在 `AnimatePresence` 外部，导致退出动画无法执行
- **修复**: 将条件判断移至 `AnimatePresence` 内部

## 验证结果
- [x] TypeScript 类型检查：通过（`npx tsc --noEmit` 0 错误）
- [x] 功能验证：通过

## 修复详情

### 1. vitals.ts
- 添加注释说明生产环境可接入真实分析服务
- 为 `console.log` 添加 `eslint-disable-next-line no-console` 标记

### 2. products-content.tsx
- 引入 `usePathname` hook 替代 `window.location.pathname`
- 将 `pathname` 提取到组件顶层，避免在 callback 中调用 hook

### 3. search-dialog.tsx
- 移除 `if (!open) return null` 提前返回
- 将 `{open && (...)}` 条件渲染包裹在 `AnimatePresence` 内部，确保退出动画正常执行

### 4. product-card.tsx
- 引入 `useRouter` hook
- 将 `window.location.href = ...` 替换为 `router.push(...)`，保持 SPA 客户端导航体验

## 需关注的技术债务
1. 图片懒加载优化（低优先级）
2. 移动端响应式细节调整（中优先级）
3. ESLint 工具链版本兼容性（中优先级）
